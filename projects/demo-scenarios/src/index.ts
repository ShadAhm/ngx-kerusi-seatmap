/**
 * The venues both demos show, and the documents behind them.
 *
 * Kept out of either demo app because they are plain Kerusi documents with no
 * framework in them: the Angular demo and the React demo showcase the same
 * five venues, and a second copy would be a second thing to keep conformant.
 * `fixtures.spec.ts` in the Angular demo is the gate that validates them.
 */
export * from './scenarios.js';
